spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: amesta
  datasource:
    url: jdbc:postgresql://localhost:15001/amesta
    username: amesta
    password: amestaadmin
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
amesta:
  app:
    jwtSecret: zEzLSiQIoOSlwlzVzEyghFCXwyMSbOsBdaLU
    jwtExpirationMs: 86400000
server:
  port: 8080